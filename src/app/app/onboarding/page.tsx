import { SimOneOnboarding } from "@/components/simone-onboarding";

export const metadata = {
  title: "Start SimOne | Sprint Zero",
};

export default function OnboardingPage() {
  return <SimOneOnboarding />;
}
